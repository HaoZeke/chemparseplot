Performance improvements:

- Added type hints to plotting modules (neb.py, geomscan.py)
- Added @lru_cache import for caching optimizations
- Added lazy imports via ensure_import() in util.py

Improves IDE support and prepares for optional dependency handling.
