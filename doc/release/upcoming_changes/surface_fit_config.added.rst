``SurfaceFitConfig`` dataclass (``from_mapping`` for TOML tables) and
``surface_fit=`` on ``plot_landscape_surface`` /
``render_single_ended_landscape``. Prefer this over growing keyword lists;
``auto_thin`` still defaults to off.
